<!DOCTYPE html>
<header>
<div class="container">
  <header class="blog-header py-3">
    <div class="row flex-nowrap justify-content-between align-items-center">
      <div class="col-4 ">
       <%= link_to image_tag("note.png",width:"120px",height:"45px"), root_path%> 
      </div>
<%- if controller_name != 'sessions' %>
     <div class="loginfont">
     <%= link_to "ログインはこちら", new_session_path(resource_name),class: "loginfont" %><br />
    </div>
    <% end %>
    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="loginfont">
  <%= link_to "会員登録はこちら", new_registration_path(resource_name),class: "loginfont" %><br />
  </div>
  <% end %>   
     </div>
    </div>
  </header>